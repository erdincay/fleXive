# Rename to testconfig.properties and substitute connection parameters to enable
# tests against a flexive REST-API service

service.url=http://localhost:8080/flexive-api-v1/api
service.username=supervisor
service.password=123456