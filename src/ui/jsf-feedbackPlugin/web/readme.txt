Place your web pages in this directory.

They will be reachable like they were deployed in the root directory
of the web application that includes this component.

Note that this is valid only for resources handled by Facelets, i.e. XHTML pages.

For images and javascripts, please use the resources/weblets directory and modify the
weblets-config.xml.